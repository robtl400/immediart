import './SkeletonCard.css';

// Grid thumbnail placeholder (the feed uses LoadingSpinner instead)
export default function SkeletonCard() {
  return <div className="skeleton-card skeleton-grid skeleton-shimmer" />;
}
